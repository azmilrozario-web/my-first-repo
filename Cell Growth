#for loop practice

# take the initial cell population as input
cells = int(input())

# take the number of days as input
days = int(input())

# for loop handles the counter automatically
for counter in range(1, days + 1):
    cells = cells * 2
    print("Day " + str(counter) + ": " + str(cells)) 